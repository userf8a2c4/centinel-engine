# Entornos virtuales
.venv/

# Salidas generadas
/data/
/hashes/
!/hashes/
/reports/
/plots/
/diffs/
analysis_results.json
analysis_results.parquet
anomalies_report.json

# Config history (auto-archived by config_loader)
command_center/configs/history/

# Archivos temporales
*.log
*.tmp
.env
.env.local
__pycache__/
node_modules/

# Databases
*.db
*.sqlite3

# Secret/key files
*.pem
*.key
secrets.yaml

# Config secrets (never commit encryption keys or tokens) /
# Secretos de config (nunca commitear claves de cifrado o tokens)
config/secrets/

# Encrypted backups (local destination) /
# Backups cifrados (destino local)
backups/
