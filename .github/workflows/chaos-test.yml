name: Chaos Tests

on:
  pull_request:

jobs:
  chaos-tests:
    runs-on: ubuntu-latest
    env:
      CHAOS_LEVEL: ${{ github.event_name == 'pull_request' && 'low' || 'medium' }}
      CHAOS_DURATION_MINUTES: ${{ github.event_name == 'pull_request' && '0.1' || '0.2' }}
    steps:
      # Checkout / Revisar código
      - name: Checkout repository / Clonar repositorio
        uses: actions/checkout@v4

      # Setup Python / Preparar Python
      - name: Set up Python / Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # Install dependencies / Instalar dependencias
      - name: Install dependencies / Instalar dependencias
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      # Run chaos tests / Ejecutar pruebas de caos
      - name: Run chaos tests / Ejecutar pruebas de caos
        run: |
          python scripts/chaos_test.py \
            --config chaos_config.yaml.example \
            --level "${CHAOS_LEVEL}" \
            --duration-minutes "${CHAOS_DURATION_MINUTES}"
